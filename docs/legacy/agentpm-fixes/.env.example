# === Local AI (LM Studio, local-first only) ===
OPENAI_API_BASE='http://127.0.0.1:1234/v1'
OPENAI_API_KEY='lm-studio'
CHAT_MODEL='qwen/qwen3-4b-2507'
EMBEDDING_MODEL='text-embedding-qwen3-embedding-0.6b'

# === Development Mode (Set to 1 to bypass LM Studio requirements) ===
MOCK_LMS=1

# === Commit policy & metrics window ===
COMMIT_SUBJECT_REGEX='^PR-[0-9]{4}\b: .+$'
METRICS_SINCE_REF='COMMIT_HYGIENE_ENFORCED'

# === Canonical folders ===
SSOT_DIR='docs/SSOT'
INSTRUCTIONS_DIR='docs/instructions'
PROOFS_DIR='docs/proofs'

# === SSOT Configuration ===
SSOT_EXPECTED_COUNT=16

# === Postgres (SSOT via URL; PG* are derived automatically) ===
DATABASE_URL='postgresql://archon_user:archon_pass@127.0.0.1:5433/agentpm'

# === Retrieval / RAG ===
EMBEDDING_DIMS='1024'
RERANKER_MODEL='Qwen3-Reranker'

# === Optional runtime knobs ===
STEP_TIMEOUT='90'
TIMEOUT_SEC='60'
LM_REQUEST_TIMEOUT_MS='30000'

# === LM Studio hardening ===
LM_AUTOLOAD_MODELS='qwen/qwen3-4b-2507,text-embedding-qwen3-embedding-0.6b'

# === Graph extraction (JSON-robust + DB-safe) ===
GRAPH_MODEL='qwen2.5-7b-instruct'
GRAPH_MAX_TOKENS='2048'
GRAPH_SAMPLE_LIMIT='0'

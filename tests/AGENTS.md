# Purpose
Test files for the entire StoryMaker system.

# Entrypoints & Commands
- Run tests: `pytest` (from project root)
- Individual tests: `pytest tests/test_name.py`

# Interfaces
- Test files with `.py` extension
- Pytest fixtures and utilities

# Constraints
- Use pytest framework
- Mock external dependencies
- Test all major functionality

# Gotchas
- Tests run in isolated environment
- Must handle missing services gracefully

# Pointers
- SSOT: `../docs/SSOT/ssot.v1.2.yaml`

# Purpose
React components for WebUI.

# Entrypoints & Commands
- Import components in parent `src/` files
- No direct execution

# Interfaces
- Export React components
- Accept props as interfaces
- Return JSX elements

# Constraints
- TypeScript interfaces required
- Tailwind CSS classes only
- No direct API calls (use lib/)

# Gotchas
- Components should be pure functions
- Use proper TypeScript types

# Pointers
- Parent: `../AGENTS.md`
- SSOT: `../../../docs/SSOT/ssot.v1.2.yaml`
